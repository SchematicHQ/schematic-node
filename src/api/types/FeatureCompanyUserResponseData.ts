// This file was auto-generated by Fern from our API Definition.

import type * as Schematic from "../index.js";

export interface FeatureCompanyUserResponseData {
    /** Whether further usage is permitted. */
    access: boolean;
    /** The maximum amount of usage that is permitted; a null value indicates that unlimited usage is permitted. */
    allocation?: number;
    /** The type of allocation that is being used. */
    allocation_type: FeatureCompanyUserResponseData.AllocationType;
    company?: Schematic.CompanyDetailResponseData;
    entitlement_id: string;
    entitlement_type: string;
    feature?: Schematic.FeatureDetailResponseData;
    /** The time at which the metric will resets. */
    metric_reset_at?: string;
    /** If the period is current_month, when the month resets. */
    month_reset?: string;
    /** The period over which usage is measured. */
    period?: string;
    plan?: Schematic.PlanResponseData;
    /** The amount of usage that has been consumed; a null value indicates that usage is not being measured. */
    usage?: number;
    user?: Schematic.UserResponseData;
}

export namespace FeatureCompanyUserResponseData {
    /** The type of allocation that is being used. */
    export const AllocationType = {
        Boolean: "boolean",
        Numeric: "numeric",
        Trait: "trait",
        Unlimited: "unlimited",
        Unknown: "unknown",
    } as const;
    export type AllocationType = (typeof AllocationType)[keyof typeof AllocationType];
}
