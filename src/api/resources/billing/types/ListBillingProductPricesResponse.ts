// This file was auto-generated by Fern from our API Definition.

import type * as Schematic from "../../../index.js";

export interface ListBillingProductPricesResponse {
    data: Schematic.BillingPriceView[];
    /** Input parameters */
    params: ListBillingProductPricesResponse.Params;
}

export namespace ListBillingProductPricesResponse {
    /**
     * Input parameters
     */
    export interface Params {
        /** Filter for prices valid for initial plans (free prices only) */
        for_initial_plan?: boolean;
        /** Filter for prices valid for trial expiry plans (free prices only) */
        for_trial_expiry_plan?: boolean;
        ids?: string[];
        interval?: string;
        /** Filter for active prices on active products (defaults to true if not specified) */
        is_active?: boolean;
        /** Page limit (default 100) */
        limit?: number;
        /** Page offset (default 0) */
        offset?: number;
        price?: number;
        product_id?: string;
        product_ids?: string[];
        q?: string;
        tiers_mode?: Schematic.BillingTiersMode;
        usage_type?: Schematic.BillingPriceUsageType;
        /** Filter for prices with a meter */
        with_meter?: boolean;
    }
}
