// This file was auto-generated by Fern from our API Definition.

import type * as Schematic from "../../../../index.js";

/**
 * @example
 *     {
 *         for_initial_plan: true,
 *         for_trial_expiry_plan: true,
 *         product_id: "product_id",
 *         interval: "interval",
 *         price: 1,
 *         q: "q",
 *         requires_payment_method: true,
 *         tiers_mode: "volume",
 *         usage_type: "licensed",
 *         limit: 1,
 *         offset: 1
 *     }
 */
export interface SearchBillingPricesRequest {
    /** Filter for prices valid for initial plans (free prices only) */
    for_initial_plan?: boolean;
    /** Filter for prices valid for trial expiry plans (free prices only) */
    for_trial_expiry_plan?: boolean;
    ids?: string | string[];
    product_id?: string;
    interval?: string;
    price?: number;
    q?: string;
    /** Filter for prices that require a payment method (inverse of ForInitialPlan) */
    requires_payment_method?: boolean;
    tiers_mode?: Schematic.SearchBillingPricesRequestTiersMode;
    usage_type?: Schematic.SearchBillingPricesRequestUsageType;
    /** Page limit (default 100) */
    limit?: number;
    /** Page offset (default 0) */
    offset?: number;
}
