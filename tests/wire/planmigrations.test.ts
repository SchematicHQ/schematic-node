// This file was auto-generated by Fern from our API Definition.

import * as Schematic from "../../src/api/index";
import { SchematicClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("PlanmigrationsClient", () => {
    test("listCompanyMigrations (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            data: [
                {
                    company_id: "company_id",
                    company_name: "company_name",
                    completed_at: "2024-01-15T09:30:00Z",
                    created_at: "2024-01-15T09:30:00Z",
                    error: "error",
                    id: "id",
                    migration_id: "migration_id",
                    plan_version_id_from: "plan_version_id_from",
                    started_at: "2024-01-15T09:30:00Z",
                    status: "completed",
                    updated_at: "2024-01-15T09:30:00Z",
                },
            ],
            params: { limit: 1000000, migration_id: "migration_id", offset: 1000000, q: "q", status: "completed" },
        };

        server
            .mockEndpoint()
            .get("/plan-version-company-migrations")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.planmigrations.listCompanyMigrations({
            migrationId: "migration_id",
            q: "q",
            status: "completed",
            limit: 1000000,
            offset: 1000000,
        });
        expect(response).toEqual({
            data: [
                {
                    companyId: "company_id",
                    companyName: "company_name",
                    completedAt: new Date("2024-01-15T09:30:00.000Z"),
                    createdAt: new Date("2024-01-15T09:30:00.000Z"),
                    error: "error",
                    id: "id",
                    migrationId: "migration_id",
                    planVersionIdFrom: "plan_version_id_from",
                    startedAt: new Date("2024-01-15T09:30:00.000Z"),
                    status: "completed",
                    updatedAt: new Date("2024-01-15T09:30:00.000Z"),
                },
            ],
            params: {
                limit: 1000000,
                migrationId: "migration_id",
                offset: 1000000,
                q: "q",
                status: "completed",
            },
        });
    });

    test("listCompanyMigrations (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-company-migrations")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.listCompanyMigrations();
        }).rejects.toThrow(Schematic.BadRequestError);
    });

    test("listCompanyMigrations (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-company-migrations")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.listCompanyMigrations();
        }).rejects.toThrow(Schematic.UnauthorizedError);
    });

    test("listCompanyMigrations (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-company-migrations")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.listCompanyMigrations();
        }).rejects.toThrow(Schematic.ForbiddenError);
    });

    test("listCompanyMigrations (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-company-migrations")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.listCompanyMigrations();
        }).rejects.toThrow(Schematic.NotFoundError);
    });

    test("listCompanyMigrations (6)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-company-migrations")
            .respondWith()
            .statusCode(500)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.listCompanyMigrations();
        }).rejects.toThrow(Schematic.InternalServerError);
    });

    test("countCompanyMigrations (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            data: { count: 1 },
            params: { limit: 1000000, migration_id: "migration_id", offset: 1000000, q: "q", status: "completed" },
        };

        server
            .mockEndpoint()
            .get("/plan-version-company-migrations/count")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.planmigrations.countCompanyMigrations({
            migrationId: "migration_id",
            q: "q",
            status: "completed",
            limit: 1000000,
            offset: 1000000,
        });
        expect(response).toEqual({
            data: {
                count: 1,
            },
            params: {
                limit: 1000000,
                migrationId: "migration_id",
                offset: 1000000,
                q: "q",
                status: "completed",
            },
        });
    });

    test("countCompanyMigrations (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-company-migrations/count")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.countCompanyMigrations();
        }).rejects.toThrow(Schematic.BadRequestError);
    });

    test("countCompanyMigrations (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-company-migrations/count")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.countCompanyMigrations();
        }).rejects.toThrow(Schematic.UnauthorizedError);
    });

    test("countCompanyMigrations (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-company-migrations/count")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.countCompanyMigrations();
        }).rejects.toThrow(Schematic.ForbiddenError);
    });

    test("countCompanyMigrations (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-company-migrations/count")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.countCompanyMigrations();
        }).rejects.toThrow(Schematic.NotFoundError);
    });

    test("countCompanyMigrations (6)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-company-migrations/count")
            .respondWith()
            .statusCode(500)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.countCompanyMigrations();
        }).rejects.toThrow(Schematic.InternalServerError);
    });

    test("listMigrations (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            data: [
                {
                    completed_at: "2024-01-15T09:30:00Z",
                    completed_companies: 1000000,
                    created_at: "2024-01-15T09:30:00Z",
                    error: "error",
                    failed_companies: 1000000,
                    id: "id",
                    plan_id: "plan_id",
                    plan_version_id_from: "plan_version_id_from",
                    plan_version_id_to: "plan_version_id_to",
                    skipped_companies: 1000000,
                    started_at: "2024-01-15T09:30:00Z",
                    status: "completed",
                    strategy: "immediate",
                    total_companies: 1000000,
                    updated_at: "2024-01-15T09:30:00Z",
                },
            ],
            params: { limit: 1000000, offset: 1000000, plan_version_id: "plan_version_id", status: "completed" },
        };

        server
            .mockEndpoint()
            .get("/plan-version-migrations")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.planmigrations.listMigrations({
            planVersionId: "plan_version_id",
            status: "completed",
            limit: 1000000,
            offset: 1000000,
        });
        expect(response).toEqual({
            data: [
                {
                    completedAt: new Date("2024-01-15T09:30:00.000Z"),
                    completedCompanies: 1000000,
                    createdAt: new Date("2024-01-15T09:30:00.000Z"),
                    error: "error",
                    failedCompanies: 1000000,
                    id: "id",
                    planId: "plan_id",
                    planVersionIdFrom: "plan_version_id_from",
                    planVersionIdTo: "plan_version_id_to",
                    skippedCompanies: 1000000,
                    startedAt: new Date("2024-01-15T09:30:00.000Z"),
                    status: "completed",
                    strategy: "immediate",
                    totalCompanies: 1000000,
                    updatedAt: new Date("2024-01-15T09:30:00.000Z"),
                },
            ],
            params: {
                limit: 1000000,
                offset: 1000000,
                planVersionId: "plan_version_id",
                status: "completed",
            },
        });
    });

    test("listMigrations (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-migrations")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.listMigrations();
        }).rejects.toThrow(Schematic.BadRequestError);
    });

    test("listMigrations (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-migrations")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.listMigrations();
        }).rejects.toThrow(Schematic.UnauthorizedError);
    });

    test("listMigrations (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-migrations")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.listMigrations();
        }).rejects.toThrow(Schematic.ForbiddenError);
    });

    test("listMigrations (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-migrations")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.listMigrations();
        }).rejects.toThrow(Schematic.NotFoundError);
    });

    test("listMigrations (6)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-migrations")
            .respondWith()
            .statusCode(500)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.listMigrations();
        }).rejects.toThrow(Schematic.InternalServerError);
    });

    test("getMigration (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            data: {
                completed_at: "2024-01-15T09:30:00Z",
                completed_companies: 1000000,
                created_at: "2024-01-15T09:30:00Z",
                error: "error",
                failed_companies: 1000000,
                id: "id",
                plan_id: "plan_id",
                plan_version_id_from: "plan_version_id_from",
                plan_version_id_to: "plan_version_id_to",
                skipped_companies: 1000000,
                started_at: "2024-01-15T09:30:00Z",
                status: "completed",
                strategy: "immediate",
                total_companies: 1000000,
                updated_at: "2024-01-15T09:30:00Z",
            },
            params: { key: "value" },
        };

        server
            .mockEndpoint()
            .get("/plan-version-migrations/plan_version_migration_id")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.planmigrations.getMigration("plan_version_migration_id");
        expect(response).toEqual({
            data: {
                completedAt: new Date("2024-01-15T09:30:00.000Z"),
                completedCompanies: 1000000,
                createdAt: new Date("2024-01-15T09:30:00.000Z"),
                error: "error",
                failedCompanies: 1000000,
                id: "id",
                planId: "plan_id",
                planVersionIdFrom: "plan_version_id_from",
                planVersionIdTo: "plan_version_id_to",
                skippedCompanies: 1000000,
                startedAt: new Date("2024-01-15T09:30:00.000Z"),
                status: "completed",
                strategy: "immediate",
                totalCompanies: 1000000,
                updatedAt: new Date("2024-01-15T09:30:00.000Z"),
            },
            params: {
                key: "value",
            },
        });
    });

    test("getMigration (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-migrations/plan_version_migration_id")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.getMigration("plan_version_migration_id");
        }).rejects.toThrow(Schematic.UnauthorizedError);
    });

    test("getMigration (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-migrations/plan_version_migration_id")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.getMigration("plan_version_migration_id");
        }).rejects.toThrow(Schematic.ForbiddenError);
    });

    test("getMigration (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-migrations/plan_version_migration_id")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.getMigration("plan_version_migration_id");
        }).rejects.toThrow(Schematic.NotFoundError);
    });

    test("getMigration (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-migrations/plan_version_migration_id")
            .respondWith()
            .statusCode(500)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.getMigration("plan_version_migration_id");
        }).rejects.toThrow(Schematic.InternalServerError);
    });

    test("countMigrations (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            data: { count: 1 },
            params: { limit: 1000000, offset: 1000000, plan_version_id: "plan_version_id", status: "completed" },
        };

        server
            .mockEndpoint()
            .get("/plan-version-migrations/count")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.planmigrations.countMigrations({
            planVersionId: "plan_version_id",
            status: "completed",
            limit: 1000000,
            offset: 1000000,
        });
        expect(response).toEqual({
            data: {
                count: 1,
            },
            params: {
                limit: 1000000,
                offset: 1000000,
                planVersionId: "plan_version_id",
                status: "completed",
            },
        });
    });

    test("countMigrations (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-migrations/count")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.countMigrations();
        }).rejects.toThrow(Schematic.BadRequestError);
    });

    test("countMigrations (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-migrations/count")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.countMigrations();
        }).rejects.toThrow(Schematic.UnauthorizedError);
    });

    test("countMigrations (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-migrations/count")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.countMigrations();
        }).rejects.toThrow(Schematic.ForbiddenError);
    });

    test("countMigrations (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-migrations/count")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.countMigrations();
        }).rejects.toThrow(Schematic.NotFoundError);
    });

    test("countMigrations (6)", async () => {
        const server = mockServerPool.createServer();
        const client = new SchematicClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { error: "error" };

        server
            .mockEndpoint()
            .get("/plan-version-migrations/count")
            .respondWith()
            .statusCode(500)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.planmigrations.countMigrations();
        }).rejects.toThrow(Schematic.InternalServerError);
    });
});
